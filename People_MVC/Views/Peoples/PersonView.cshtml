@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@model Person;

@{
}


    <td>@Model.Name</td>
    
    <td>@Model.City.Name @Model.City.Country.Name</td>
    <td>@Model.TeleNumber</td>

    <td>
        @foreach (var language in @Model.PersonLanguages)
        {
            <p> @Model.Name</p>
        }
    </td>
    <td><a class="btn btn-danger" asp-controller="People" asp-action="RemovePerson" asp-route-id="@Model.PersonId">Remove</a></td>
    <td>
        <button id="@Model.PersonId" class="edit-btn btn btn-primary">
            Edit
        </button>
    </td>


